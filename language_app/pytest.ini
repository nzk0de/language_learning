# pytest.ini
[pytest]
markers =
    unit: marks fast, isolated unit tests (no external services)
    integration: marks slower tests that load models or hit services
    e2e: marks full end-to-end tests
filterwarnings =
    ignore::DeprecationWarning